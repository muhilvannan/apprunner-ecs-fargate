---
phase: 01-infra-swap-drop-envoy-wire-cloud-map
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - controller-python/main.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "Workspace task definition registers only the landing-page container — no Envoy container present"
    - "No emptyDir shared volume in workspace task definition registration call"
    - "ALB target group and TG registration use LANDING_PAGE_PORT (3001) not ENVOY_PORT (8080)"
    - "ENVOY_PORT, ENVOY_ADMIN_PORT constants and _ENVOY image constant are removed from controller"
    - "Envoy environment variable (ENVOY_ADMIN_PORT) removed from landing-page container env"
  artifacts:
    - path: "controller-python/main.py"
      provides: "Single-container workspace task definition, TG port fixed to 3001"
      contains: "landing-page"
  key_links:
    - from: "register_workspace_task_definition"
      to: "ecs.register_task_definition"
      via: "container_defs list with single landing-page entry"
      pattern: "container_defs.*landing-page"
    - from: "ensure_workspace_target_group"
      to: "LANDING_PAGE_PORT"
      via: "Port= argument"
      pattern: "Port=LANDING_PAGE_PORT"
---

<objective>
Remove Envoy from the workspace task definition. After this plan, `register_workspace_task_definition` produces a single-container task (landing-page only) with no emptyDir volume, and the ALB target group points to port 3001 (landing page) instead of 8080 (Envoy).

Purpose: Clears the Option B (Envoy) infrastructure so Option C (Cloud Map + Node.js proxy) can be wired in.
Output: Updated `controller-python/main.py` — clean single-container workspace task, correct TG port.
</objective>

<execution_context>
@/Users/muhil-work/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhil-work/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@controller-python/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip Envoy container and emptyDir volume from workspace task definition</name>
  <files>controller-python/main.py</files>
  <action>
    In `register_workspace_task_definition` (line ~254):

    1. Remove the `envoy_cmd` variable entirely (the list starting with "until [ -f /etc/envoy...")
    2. Remove the second container dict (name="envoy", image=_ENVOY, portMappings with ENVOY_PORT/ENVOY_ADMIN_PORT, command=envoy_cmd, mountPoints, dependsOn, logConfiguration) from `container_defs`
    3. Remove `"mountPoints": [{"sourceVolume": "envoy-config", ...}]` from the landing-page container dict
    4. Remove `"ENVOY_ADMIN_PORT"` entry from the landing-page container's `environment` list
    5. Change `volumes = [{"name": "envoy-config"}]` to `volumes = []` — no volumes needed
    6. Pass `volumes=[]` (or remove the `volumes=volumes` kwarg entirely) in the `ecs.register_task_definition` call — AWS accepts empty list

    Also clean up now-unused constants at the top of the file:
    - Delete line: `ENVOY_PORT         = 8080`
    - Delete line: `ENVOY_ADMIN_PORT   = 9901`
    - Delete line: `_ENVOY   = "public.ecr.aws/envoyproxy/envoy:v1.29-latest"`

    Update the docstring on `register_workspace_task_definition` from
    "landing-page + envoy containers. Shares an emptyDir volume for the Envoy config file."
    to
    "landing-page container only (Option C: Cloud Map + Node.js proxy)."

    Update `workspace_task_family` docstring from "(landing page + envoy)" to "(landing page only)".
  </action>
  <verify>
    Run: `python -c "import ast, sys; ast.parse(open('controller-python/main.py').read()); print('syntax ok')"` from the repo root.
    Also grep to confirm removal: `grep -n "envoy\|emptyDir\|ENVOY_PORT\|ENVOY_ADMIN_PORT\|_ENVOY" controller-python/main.py` — should return zero matches (case-sensitive).
  </verify>
  <done>
    `controller-python/main.py` parses without error; no occurrences of ENVOY_PORT, ENVOY_ADMIN_PORT, _ENVOY, or emptyDir in the file; `register_workspace_task_definition` builds a single-container definition with no mountPoints and empty volumes list.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix ALB target group and TG registration to use LANDING_PAGE_PORT</name>
  <files>controller-python/main.py</files>
  <action>
    Two locations reference `ENVOY_PORT` for the target group — both must point to `LANDING_PAGE_PORT` (3001) because Envoy is removed and the ALB now routes directly to the landing page.

    1. In `ensure_workspace_target_group` (~line 506):
       - Change log line: `"Creating workspace target group: %s port=%d", tg_name, ENVOY_PORT`
         → `"Creating workspace target group: %s port=%d", tg_name, LANDING_PAGE_PORT`
       - Change `Port=ENVOY_PORT` → `Port=LANDING_PAGE_PORT` in `elbv2.create_target_group` call
       - Update the comment/docstring: change "pointing at Envoy port" → "pointing at landing page port"

    2. In `register_workspace_to_tg` (~line 627):
       - Change the docstring: "Register the workspace service task IP:ENVOY_PORT to the workspace TG."
         → "Register the workspace service task IP:LANDING_PAGE_PORT to the workspace TG."
       - Change log line: `"Registering workspace task %s:%d → TG", ip, ENVOY_PORT`
         → `"Registering workspace task %s:%d → TG", ip, LANDING_PAGE_PORT`
       - Change `Targets=[{"Id": ip, "Port": ENVOY_PORT}]`
         → `Targets=[{"Id": ip, "Port": LANDING_PAGE_PORT}]`

    3. Update the comment on `ensure_workspace_listener_rule` docstring:
       "Always forward — Envoy handles the per-app routing internally."
       → "Always forward — landing page handles per-app routing via Cloud Map proxy."

    4. Update the `arch` field in the `/health` route response from `"per-app-run_task+envoy"` → `"per-app-run_task+cloudmap"`.
  </action>
  <verify>
    Run: `python -c "import ast, sys; ast.parse(open('controller-python/main.py').read()); print('syntax ok')"` from repo root.
    Confirm: `grep -n "ENVOY_PORT\|ENVOY_ADMIN_PORT" controller-python/main.py` returns zero results.
    Confirm: `grep -n "LANDING_PAGE_PORT" controller-python/main.py` includes lines inside `ensure_workspace_target_group` and `register_workspace_to_tg`.
  </verify>
  <done>
    No references to ENVOY_PORT or ENVOY_ADMIN_PORT remain anywhere in main.py; ALB target group is created on port 3001; TG target registration uses port 3001; Python syntax check passes.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `python -c "import ast; ast.parse(open('controller-python/main.py').read()); print('ok')"` — no syntax errors
2. `grep -c "envoy\|ENVOY\|emptyDir" controller-python/main.py` → 0 (case-sensitive search excluding comments that reference Option B for historical context)
3. `grep "Port=LANDING_PAGE_PORT" controller-python/main.py` → present in ensure_workspace_target_group
4. The `register_workspace_task_definition` function's `container_defs` list has exactly 1 entry (landing-page)
</verification>

<success_criteria>
- INFRA-01: `register_workspace_task_definition` produces a single-container task (landing-page only)
- INFRA-02: No emptyDir volume in workspace task definition (volumes=[])
- INFRA-03: Envoy constants (ENVOY_PORT, ENVOY_ADMIN_PORT, _ENVOY) removed; TG port fixed to LANDING_PAGE_PORT
- Python syntax check passes on modified main.py
</success_criteria>

<output>
After completion, create `.planning/phases/01-infra-swap-drop-envoy-wire-cloud-map/01-01-SUMMARY.md`
</output>
