---
phase: 02-controller
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - controller/src/index.ts
  - controller/src/routes/workspace.ts
  - controller/src/routes/app.ts
  - controller/src/services/awsClient.ts
  - controller/src/config.ts
  - controller/Dockerfile
  - controller/docker-compose.yml
  - controller/package.json
  - controller/README.md
autonomous: true
requirements: [API-04]

must_haves:
  truths:
    - "Local controller API starts and accepts requests at http://localhost:3000"
    - "AWS SDK client is configured via environment variables"
    - "API has clear error handling and logging for all requests"
    - "Docker compose starts the controller service with correct environment mapping"
  artifacts:
    - path: "controller/src/index.ts"
      provides: "Express app entrypoint"
      min_lines: 20
    - path: "controller/Dockerfile"
      provides: "Container build instructions"
      contains: "FROM node"
    - path: "controller/docker-compose.yml"
      provides: "Local service orchestration for controller"
      contains: "service: controller"
    - path: "controller/package.json"
      provides: "Project dependencies and scripts"
  key_links:
    - from: "controller/src/index.ts"
      to: "controller/src/routes/workspace.ts"
      via: "app.use('/api/workspace', workspaceRouter)"
      pattern: "app\.use\('/api/workspace'"
    - from: "controller/src/services/awsClient.ts"
      to: "controller/src/routes/workspace.ts"
      via: "ECS/CloudMap/S3 client usage"
      pattern: "new (ECS|ServiceDiscovery|IAM|S3)" 
---

<objective>
Build the local controller scaffold in Node.js/Express and Docker so the API can be developed and run locally. 

Purpose: Provide a runnable controller app with AWS SDK wiring and bootstrappable workspace/app endpoints.

Output: `controller/` application with initial routes and containerization.
</objective>

<tasks>
<task type="auto">
  <name>Initialize controller project scaffold</name>
  <files>
    controller/package.json
    controller/src/index.ts
    controller/src/config.ts
  </files>
  <action>
Create a Node 20+ Express project. 
- package.json includes dependencies: express, aws-sdk v3 packages (client-ecs, client-servicediscovery, client-efs, client-s3, client-iam), dotenv, pino. 
- scripts: start, dev (nodemon), lint.
- config.ts exports env var parser (AWS_REGION, CLUSTER_NAME, CLOUDBINDS)
- index.ts sets up Express app, JSON parser, basic /healthz endpoint.

Avoid: Hard-coded AWS credentials in code.
  </action>
  <verify>
cd controller && npm install && npm run lint
node -e "require('./dist/index')" is not required, just ensure npx tsc compile passes.
  </verify>
  <done>
Controller skeleton runs with `npm run start`, and GET /healthz returns 200.
</done>
</task>

<task type="auto">
  <name>Add controller Docker files</name>
  <files>
    controller/Dockerfile
    controller/docker-compose.yml
  </files>
  <action>
Create Dockerfile with node:20 image, copy package, install, build, and run. 
Create docker-compose exposing 3000 and mapping local env file. 

Ensure `docker-compose up --build` runs and container logs show app startup.
  </action>
  <verify>
cd controller && docker-compose up --build --abort-on-container-exit --exit-code-from controller

Expected: container starts and says "Server running on port 3000".
  </verify>
  <done>
Docker container starts and /healthz is accessible.
</done>
</task>

</tasks>

<verification>
1. API up at http://localhost:3000/healthz returns {"status":"ok"}. 
2. `docker-compose up` builds successfully. 
3. AWS SDK client can be created from config file with env variables.
</verification>

<success_criteria>
- [ ] Controller code compiles (tsc) and Express startup succeeds.
- [ ] Health endpoint exists.
- [ ] Docker setup works.
- [ ] Requirements API-04 satisfied.
</success_criteria>

<output>
After completion, create .planning/phases/02-controller/02-01-SUMMARY.md with scaffold details.
</output>
